//! Access context: `VaultGrant` — who holds a wrapped copy of which vault
//! key, with what role.
//!
//! Fleshed out in Phase 2 with exactly one grant per vault (the owner);
//! sharing in v2 adds grants without touching vault data.
